{
  "criteria": [
    {
      "code": "has_select_statement",
      "category": "technical",
      "applies_to": "sql",
      "severity": "required",
      "title": "В запросе есть SELECT",
      "description": "Ожидается SQL-запрос с SELECT",
      "detection_mode": "rule",
      "rule": {
        "artifact_types": [
          "sql_query"
        ],
        "patterns_any": [
          "select"
        ]
      },
      "comment_templates": {
        "pass": "SELECT найден.",
        "fail": "Похоже, в файле не найден корректный SELECT-запрос."
      }
    },
    {
      "code": "nullif_division_guard",
      "category": "technical",
      "applies_to": "sql",
      "severity": "required",
      "title": "Есть защита от деления на ноль",
      "description": "Если в запросе есть деление, добавь NULLIF(..., 0)",
      "detection_mode": "hybrid",
      "hybrid_check": {
        "task": "dangerous_division_guard"
      },
      "comment_templates": {
        "pass": "Защита через NULLIF найдена.",
        "fail": "Если в запросе есть деление, добавь защиту от деления на ноль через NULLIF(..., 0)."
      }
    },
    {
      "code": "join_logic_present",
      "category": "technical",
      "applies_to": "sql",
      "severity": "warning",
      "title": "Есть логика объединения таблиц",
      "description": "Если задача многотабличная, проверь корректность JOIN",
      "detection_mode": "rule",
      "rule": {
        "artifact_types": [
          "sql_query"
        ],
        "patterns_any": [
          " join "
        ]
      },
      "comment_templates": {
        "pass": "JOIN найден.",
        "fail": "Если задача требует объединения таблиц, проверь наличие и корректность JOIN."
      }
    }
  ]
}